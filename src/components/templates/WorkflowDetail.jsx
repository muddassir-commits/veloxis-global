import React from 'react';
import IndustryAutomationDetail from './IndustryAutomationDetail';

/**
 * WorkflowDetail Programmatic Template
 * 
 * Proxies dynamically to IndustryAutomationDetail for structural compliance,
 * ensuring robust rendering architecture for explicit workflow taxonomies.
 */
const WorkflowDetail = (props) => {
  return <IndustryAutomationDetail {...props} />;
};

export default WorkflowDetail;
